#include <stdio.h>
int main()
{
int num;
printf("inserisci il numero da verificare se è negativo o positivo : ");
scanf("%d", &num);
if (num<0)
printf("il numero %d è negativo.", num);
else
printf("il numero %d è positivo.", num);










}